# powerplant-smt-api
Coding Challenge for ENGIE Application. The API should provide an endpoint which, given a list of power plants with costs, minimums and maximus, returns the production configuration to grant minimal cost
